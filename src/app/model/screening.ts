import { Cinema } from "./cinema";

export class Screening {

    //startDateTime: string;
   // cinema: Cinema;

    constructor(startDateTime: string, cinema: Cinema) {
     //   this.startDateTime = startDateTime;
     //   this.cinema = cinema;
    }

}